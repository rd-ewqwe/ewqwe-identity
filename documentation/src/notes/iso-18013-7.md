Navigating ISO specifications and EUDIW implementation profiles is no small feat—there is a lot of overlapping terminology, shifting drafts, and complex cryptography involved. I can certainly provide you with a comprehensive technical report to clarify the architecture and guide your codebase architecture.

Here is your detailed technical report on the ISO/IEC 18013-7 request mechanisms, the France Identité wallet implementation status, and the interoperability profiles.

---

## Technical Report: ISO/IEC 18013-7 Request Building Mechanisms

ISO/IEC 18013-7 standardizes how mobile driving licenses (mDLs) and generic mDocs are presented over the internet, extending the proximity-based protocols of ISO/IEC 18013-5. The standard defines three distinct annexes for remote presentation, each requiring a fundamentally different approach to building the Verifier's request.

### 1. Annex A: REST API (Device Retrieval)

Annex A relies on an HTTP-based remote device retrieval paradigm. It essentially takes the core ISO 18013-5 CBOR/COSE cryptographic structures and routes them over a REST API rather than a local BLE/NFC connection.

* **Request Construction:** The Verifier application builds an HTTP POST request.
* **Payload Format:** The body contains a CBOR-encoded `DeviceRequest` exactly as specified in ISO/IEC 18013-5. This includes the requested document types, namespaces, and specific data elements, wrapped in session encryption.
* **Transport:** The request is sent directly to a server acting as a proxy for the mDL, or directly to a wallet's exposed endpoint.
* **Developer Impact:** Implementing Annex A in code requires heavy lifting in CBOR parsing, COSE cryptography, and session key establishment directly at the network layer.

### 2. Annex B: OpenID4VP (OpenID for Verifiable Presentations)

Annex B standardizes mDoc presentation over the OAuth 2.0 / OpenID Connect framework. This is the dominant protocol in the broader verifiable credential ecosystem.

* **Request Construction:** The Verifier acts as an OAuth 2.0 Client. The request is built as an Authorization Request URI (typically leveraging SIOPv2/OID4VP).
* **Payload Format:** The core of the request is a `presentation_definition` object (using Presentation Exchange - PEX). This JSON object maps the required mDoc claims (e.g., `org.iso.18013.5.1` namespace) to the Verifier's requirements.
* **Transport:** The request is often delivered via a `request_uri` pointing to a JWT Secured Authorization Request (JAR). The wallet retrieves this, gathers user consent, and sends an encrypted `vp_token` containing the mDoc `DeviceResponse` to the Verifier's `response_uri` via HTTP `direct_post`.
* **Developer Impact:** Requires robust JWT/JWE handling and implementation of Presentation Exchange logic, but avoids the need to build custom network routing for CBOR payloads.

### 3. Annex C: W3C Digital Credentials API (DC API)

Annex C moves the request mechanism out of the backend network layer and into the client-side browser using a standardized Web API.

* **Request Construction:** The request is built in client-side JavaScript using the `navigator.credentials.get()` method.
* **Payload Format:** The request specifies a `digital` credential type. It encapsulates the protocol-specific request profile (e.g., nesting an OID4VP request or a direct `org-iso-mdoc` profile) directly inside the browser API call.
* **Transport:** The browser and the underlying operating system natively handle wallet invocation. For cross-device flows, the OS establishes a secure tunnel (utilizing BLE proximity checks and relay servers like `cable.ua5v.com`) to pass the request to the wallet.
* **Developer Impact:** Drastically reduces Verifier UX complexity. Developers rely on the browser to handle context switching, session binding, and cross-device tunneling.

---

## France Identité Wallet: Implementation Status

Based on the 2026 European Digital Identity Wallet (EUDIW) Unfold Playground and current technical deployments, the France Identité wallet supports the following ISO/IEC 18013-7 protocols:

| Standard | Status in France Identité | Implementation Details |
| --- | --- | --- |
| **Annex B (OpenID4VP)** | **Implemented** | Supported for remote online verification. The wallet handles OID4VP 1.0 (previously draft 18) requests using Presentation Exchange to return EUDI PIDs and Age Verification attestations. |
| **Annex C (DC API)** | **Implemented** | Actively supported as a DC API Verifier. The wallet integrates with the OS to intercept browser-mediated `navigator.credentials.get()` requests containing `dc_api.jwt` OID4VP payloads. |
| **Annex A (REST API)** | **Not Implemented** | France Identité does not utilize the pure REST/CBOR device retrieval model defined in Annex A, favoring the OpenID/W3C web-standard approaches. |

---

## OpenID4VP HAIP vs. EU-AV Schemes

While both the High Assurance Interoperability Profile (HAIP) and the EU Age Verification (EU-AV) blueprint leverage OpenID4VP, they have fundamentally different threat models and architectural rules. Implementing these in code requires distinct request parameter configurations.

### OpenID4VP HAIP (High Assurance Interoperability Profile)

Managed by the OpenID Foundation, HAIP is designed for generalized, high-security credential exchange.

* **Integrity Protection:** HAIP strictly relies on JWT Secured Authorization Requests (JAR) to protect the Verifier's request integrity.
* **Trust Model:** It assumes the existence of Relying Party (RP) Trust Lists. The Wallet uses PKI/X.509 certificates to verify the Verifier's identity before presenting the mDoc.
* **Response:** Demands highly encrypted responses utilizing `direct_post.jwt`.

### EU-AV Scheme (EU Age Verification Blueprint)

Driven by the Digital Services Act (DSA), the EU-AV blueprint is an EUDIW-specific profile designed *exclusively* for privacy-preserving, anonymous age checks (e.g., proving "Over 18" for online services).

* **Privacy by Design:** Employs Zero-Knowledge Proofs (ZKPs) operating on a double-blind model. The Verifier receives a cryptographic binary "yes/no" without learning the user's identity, and the Issuer cannot see where the credential is being used.
* **Deliberate Rejection of JAR:** In code, **EU-AV explicitly drops the HAIP JAR requirement**. Because the AV ecosystem is inherently decentralized and lacks a master Relying Party Trust List, an attacker could easily obtain a valid certificate from a rogue CA and substitute a legitimate JAR with a malicious one.
* **Protocol Priority:** Prioritizes ISO/IEC 18013-7 Annex C (DC API) for seamless, platform-mediated presentation, utilizing Annex B (standard OpenID4VP) strictly as a fallback mechanism.

# **EUDIW Architectural Implementation Matrix (May 2026\)**

This document provides a detailed technical comparison of the ISO/IEC 18013-7 request mechanisms and European Digital Identity Wallet (EUDIW) implementation profiles across major European member states.

## **Implementation Matrix**

| Scheme / Architecture | Transport / Invocation Layer | Request / Payload Format | Trust & Security Model | France (France Identité) | Germany (EUDI Wallet DE) | Italy (IT Wallet) | Spain (Spanish EUDIW)   |
| :---- | :---- | :---- | :---- | :---- | :---- | :---- | :---- |
| **ISO/IEC 18013-7 Annex A** | HTTP REST API (POST) direct to endpoint. | CBOR DeviceRequest / DeviceResponse. | Network-layer session encryption. No global trust list. | **Not Implemented** | **Not Implemented** | **Not Implemented** | **Not Implemented** |
| **ISO/IEC 18013-7 Annex B** | Universal/Deep links (openid4vp://), HTTP direct\_post. | **DCQL** (legacy PEX supported as fallback in some environments). | JWT Secured Authorization Request (**JAR**) & standard TLS. | **Implemented** (Production). Uses DCQL & JAR. | **Implemented** (Beta/Sandbox). Uses DCQL & JAR. | **Implemented** (Production/IO App). Uses DCQL & JAR. | **Implemented** (Pilot). Uses DCQL & JAR. |
| **ISO/IEC 18013-7 Annex C** | Browser API (navigator.credentials.get); BLE/WebSocket tunnels. | Envelopes an OID4VP request (e.g., dc\_api.jwt). | Trust based on web origins and platform-verified App Links. | **Implemented**. Wraps DCQL. Adapts JAR presence based on flow. | **Implemented**. Wraps DCQL via browser mediated flows. | **Implemented**. Wraps DCQL. Deeply integrated via OS-level wallet. | **Implemented**. Heavily prioritized for web-based pilots. |
| **OpenID4VP HAIP** | OpenID4VP (Annex B), custom schemes, direct\_post.jwt. | **DCQL** (Mandated by EUDIW). | **Strict JAR:** Verifier MUST authenticate via EU Trust List x.509 certificates. | **Implemented**. Enforces DCQL & strict JAR requirements. | **Implemented**. Enforces DCQL & strict JAR requirements. | **Implemented**. Enforces DCQL & strict JAR requirements. | **Implemented**. Enforces DCQL & strict JAR requirements. |
| **EUDIW EU-AV Blueprint** | Prioritizes **Annex C** (DC API); fallback to Annex B. | **DCQL** optimized for minimal disclosure (Boolean ZKPs). | **No JAR:** Deliberately drops JAR to prevent Verifier tracking. | **Implemented**. Intentionally drops JAR for privacy. | **Implemented**. Intentionally drops JAR for privacy. | **Implemented**. Intentionally drops JAR for privacy. | **Implemented**. Core focus of current age-assurance pilots. Drops JAR. |

## **Glossary of Acronyms**

* **API:** Application Programming Interface  
* **BLE:** Bluetooth Low Energy (used by operating systems to prove physical proximity in cross-device flows).  
* **CBOR:** Concise Binary Object Representation (a binary data serialization format used heavily in ISO mDocs for compact cryptographic payloads).  
* **DC API:** Digital Credentials API (a W3C browser standard for requesting credentials directly through the web browser).  
* **DCQL:** Digital Credential Query Language (the modern, JSON-based query language adopted by the EUDIW to request specific data from wallets, replacing PEX).  
* **DSA:** Digital Services Act (EU regulation driving the EU-AV blueprint for mandatory, anonymous age verification).  
* **ECDH:** Elliptic Curve Diffie-Hellman (a key agreement protocol used to establish a shared secret for encrypted sessions).  
* **EUDIW:** European Digital Identity Wallet.  
* **EU-AV:** European Union Age Verification (the specific EUDIW blueprint/scheme for privacy-preserving age checks).  
* **HAIP:** High Assurance Interoperability Profile (an OpenID profile ensuring strict security and trust requirements for verifiable presentations).  
* **HPKE:** Hybrid Public Key Encryption (a modern cryptographic standard for encrypting payloads to a public key).  
* **HTTP:** Hypertext Transfer Protocol.  
* **ISO/IEC:** International Organization for Standardization / International Electrotechnical Commission.  
* **JAR:** JWT Secured Authorization Request (RFC 9101). A standard where the Verifier's request is cryptographically signed to prove its authenticity. Strongly required by HAIP but explicitly rejected by EU-AV.  
* **JSON:** JavaScript Object Notation.  
* **JWE:** JSON Web Encryption (used to encrypt the wallet's response back to the Verifier).  
* **JWT:** JSON Web Token (a standard for securely transmitting information as a JSON object).  
* **mDoc:** Mobile Document (the data model format standardized by ISO/IEC 18013-5, natively using CBOR).  
* **mDL:** Mobile Driving License.  
* **OIDC:** OpenID Connect (an identity layer built on top of the OAuth 2.0 framework).  
* **OID4VP:** OpenID for Verifiable Presentations (the specific standard for presenting credentials over OIDC).  
* **OS:** Operating System.  
* **PEM:** Privacy-Enhanced Mail (a widely used file format for storing cryptographic keys and certificates).  
* **PEX:** Presentation Exchange (a legacy JSON query language used in earlier verifiable credential flows, largely replaced by DCQL in EUDIW).  
* **PID:** Personal Identity Data (the core identity credential inside the EUDIW).  
* **REST:** Representational State Transfer.  
* **SD-JWT:** Selective Disclosure JSON Web Token (a credential format allowing the user to reveal only specific claims, rather than the whole document).  
* **TLS:** Transport Layer Security.  
* **URI:** Uniform Resource Identifier.  
* **VC:** Verifiable Credential.  
* **ZKP:** Zero-Knowledge Proof (a cryptographic method allowing one party to prove a statement without revealing any other underlying data).
