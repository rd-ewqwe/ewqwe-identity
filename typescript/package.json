{
  "name": "@ewqwe/workspace",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "pnpm --recursive build",
    "dev": "pnpm --recursive --parallel dev",
    "test": "pnpm --recursive test",
    "typecheck": "pnpm --recursive typecheck",
    "lint": "pnpm --recursive lint"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  },
  "overrides": {
    "vite": ">=6.4.3",
    "esbuild": ">=0.24.3"
  }
}
